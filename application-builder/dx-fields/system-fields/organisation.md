# 🏢 Organisation

The **Organisation** field allows users to select a business entity from a predefined list within the system.&#x20;

✅ Why It’s Important:

* Identifies which company or client a record belongs to
* Essential for managing data in multi-tenant environments

<figure><img src="../../../.gitbook/assets/New Project (24).jpg" alt=""><figcaption></figcaption></figure>

🪄 **Default Value: Fill with Current User’s Organisation**\
This field supports an intelligent default configuration:

* **Option**: Fill with Current User’s Organisation
* **Purpose**: Automatically selects the organisation linked to the logged-in user
* **Use Case**: Ideal for workflows where users should only operate within their assigned organisation
* **Overrides**: Can be overridden via rules or conditional logic, if needed

